pipeline {
  agent {
      docker { 
            image 'jenkins/jenkins:latest' 
            label 'linux'
             }
  }
  stages {
    stage('Hello') {
      steps {
        sh '''
          ansible --version
          ansible-playbook --version
          ansible-galaxy --version
        '''
      }
    }
  }
}
